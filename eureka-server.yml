logging:
  file: ${spring.application.name}.log
  path: ../

server:
  address: ${CONF_EUREKA_HOST:0.0.0.0}
  port: ${CONF_EUREKA_PORT:8762}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
